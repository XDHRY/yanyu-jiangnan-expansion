# 给后续模型的具体任务

148张任务卡按assets、districts、materials、systems、assemblies分类。每张均可独立打开阅读。

每次从[TASK_INDEX](../TASK_INDEX.md)领取一个编号；阅读依赖，再按文件指定路径实现。具体物体由A任务负责，材料由M任务负责，多物体关系由K任务负责，区域功能由D任务负责，共同规则由S任务负责。

当前所有任务只有规格，不是已完成结果。未完成的上游可以显式占位，不可虚报实现。
