# A072 · 竹制扫帚 · 后续 AI 实现提示词

**状态：待实现。** 本文件定义目标资产，现有Python骨架即使包含同类体块，也不代表本任务细节已完成。
返回 [任务索引](../../TASK_INDEX.md) · [总指导](../../MASTER_GUIDE.md)。

## 任务边界与交付路径

- 资产编号：`JNX_A072`；优先级：`P1`；服务区域：`D03, D05, D06`。
- 新增实现：`expansion/future/props/bamboo_broom.py`。现阶段该路径是交付目标，不是已经存在的脚本。
- 设计标称包围尺寸：X宽 0.5 m × Y深 0.25 m × Z高 1.5 m。这是艺术与接口起点，不是考古测绘结论；可以在分配的地块内合理参数化。
- 新结果放入 `expansion/generated/A072/v001/`：`bamboo_broom.blend`、`asset_manifest.json`、`preview_front.png`、`preview_three_quarter.png`、`NOTES.md`。随后版本新增 `v002/`，保留旧结果。
- 先读 `docs/02_spatial_contract.md`、`docs/03_python_contract.md`、`docs/04_asset_contract.md`。本任务不修改 `jiangnan.py`、旧 `Jiangnan.blend` 或旧资产数据库。

## 可直接交给模型的主提示词

你正在为“烟雨江南”建立可编辑的Blender 4.5资产“竹制扫帚”。风格延续历史写实江南、粉墙黛瓦、湿润空气与克制武侠；参考《燕云十六声》的空间气韵，以及《黑神话：悟空》的材质分量与远近层次。输出原创构造，不把概念图当模型。

具体形态：细柄束枝与绑带，常倚墙放置，枝端使用后散开。尺度基准采用 0.5×0.25×1.5 米；先输出正面、侧面与俯视可解释的结构，再在独立子对象中添加细节。所有主要部件必须有名称，能被下一位AI定位。参数包括尺寸、湿润度、旧化程度、seed与variant；同一seed产生相同配置，不让其他资产的生成顺序改变本资产。

几何实现路线：柄用圆柱，扫枝曲线成束，绑带位于受力处。几何要有厚度、内外侧与必要的真实孔洞；细节按照镜头距离分配，不用全面细分替代结构判断。材质要求：以用途选择木、陶、布、纸或铁；常摸的位置与藏灰的位置区别表达。 必须分别说明哪些已经建模，哪些只是占位，哪些留给后续贴图任务。完成一版后保存产物与说明，不擅自反复改风格或扩张本任务范围。

## 建模步骤

1. 写出参数表，计算包围盒与原点；原点放在主要落地支撑面的中心，Z朝上，正面朝局部-Y。水上资产另以设计水线为基准，并在manifest明确记录。
2. 按上述形态建立承重/支撑层，再建立外形轮廓层；区分真实构件、装饰附件与替代体，不把全部网格合成一个不可追踪对象。
3. 依照实现路线生成子部件。实际实施时使用 `build(builder, instance)` 接口；可复用 `kernel.py` 的box、cylinder、roof、mesh，复杂网格自行生成顶点与面再交给builder。
4. 建立下述连接点，将各点的局部坐标、朝向、容差和用途写入manifest；连接点是可见空对象或可转换为其的结构数据。
5. 为本资产制作 intact_damp / used_damp / repaired_damp 三个可选变体：分别表达完整但潮湿、常用磨损、局部修补。三者保持接口和总体尺寸一致；涉及自然资产时对应健康、老化、修枝/岸线修复语义。
6. 按镜头等级完善UV、贴图引用、LOD与简化碰撞；单次交付仅声明实际完成的层级。LOD0暂定0.5k–6k三角面；近景特写另立版本，普通道具共享材质。
7. 保存新版本并提供清楚的实现说明：调用了什么接口、如何生成主体、如何接入相邻资产、存在什么限制。

## 接口与咬合

grip接手握位，lean接墙面。最低限度同时提供 `origin` 与 `bounds` 元数据。所有接入点使用局部坐标，通过实例根节点的平移和绕Z旋转转成世界坐标；不要把区域中心写死在资产源文件里。

与道路交接：地面基准为Z=2.0米；道路面比地面高0.12米；水面Z=0。接路的台阶或坡道需要在连接处吸收这0.12米差值。近邻独立物体保留至少0.05米避免意外共面；建筑落地和榫接等有意接触在manifest中标注。

与其他任务交接：先完成S02坐标、S03连接点、S04材质约定，再实现本任务。细化时保留`JNX_A072`及稳定子件后缀；禁止通过改动相邻任务的尺寸掩盖自己的接口错误。

## 针对本资产的完成标准

扫枝数量有LOD预算，倚靠有接触点，柄不穿墙。另外要求：尺寸与记录一致、主要部件可选择、无丢失贴图、对象层级可追踪、两个预览确实由该.blend产生。未做的UV、LOD、碰撞、动画分别写“待完成”；概念图、目标图与真实渲染分开命名。

## 实现者必须回填的说明

在产物目录`NOTES.md`写：本次新增文件；参数默认值及范围；采用的Blender/Python机制；连接点表；实际对象/面数；可重建命令；尚未做的内容。不得只写“已完成”或把该提示词原样复制作为实现报告。
