# 规划配置

world.json声明世界边界、统一标高、12地区、家族任务映射、旧项目基线与只读接入约定。
connections.json声明17条相邻地区连接，桥的跨度、宽度和世界中心。

当前layout.py按相邻地区中心生成等价桥连接；若未来更改连接拓扑，必须先实现从connections.json驱动layout的适配，不能只改其中一处就以为另一处自动更新。

不把建筑/树木配置数量报告成已运行产物。未来以scene_manifest中实际生成的结果统计为准。
